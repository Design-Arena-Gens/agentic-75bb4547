# Analyse de la fermeture de l'espace aérien

Application Next.js présentant une note d'analyse géopolitique sur la décision conjointe du Venezuela, de Cuba et de l'Iran de fermer leur espace aérien aux appareils américains.

## 🚀 Prise en main

```bash
npm install
npm run dev
```

Ensuite ouvrir http://localhost:3000.

## 🧱 Structure

```
app/
  layout.js        # Mise en page globale et métadonnées
  page.js          # Contenu principal de l'analyse
  globals.css      # Styles globaux
```

## 🧪 Scripts

- `npm run dev` : serveur de développement
- `npm run build` : build de production
- `npm start` : serveur Next.js en production
- `npm run lint` : lint via ESLint

## 📄 Licence

MIT
